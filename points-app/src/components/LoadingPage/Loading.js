import React from 'react'
import { Loader, LoadingContainer } from './LoadingElements'

const Loading = () => {
    return (
        <LoadingContainer>
            <Loader>
                
            </Loader>
        </LoadingContainer>
    )
}

export default Loading
